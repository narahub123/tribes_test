export interface ComuLeaderProfile {
  id: string;
  avatarUrl: string;
  role: string;
  name: string;
  job: string;
}

export interface ComuNoticeType {
  id: number;
  title: string;
  author: string;
  createdAt: string;
}
