import { ComuSectionHeader } from "../ComuSectionHeader";

export const NoticeSection = () => {
  return (
    <section>
      <ComuSectionHeader title={"공지"} className="" />
    </section>
  );
  ``;
};
